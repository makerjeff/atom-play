// JOIN_LOGIC.JS
let join_form         = document.getElementById('join_form');
let join_form_input   = join_form.children[0];
let join_form_button  = join_form.children[1];





join_form.addEventListener('submit', (ev) => {
  ev.preventDefault();
});
